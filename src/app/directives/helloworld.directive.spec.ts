import { HelloworldDirective } from './helloworld.directive';

describe('HelloworldDirective', () => {
  it('should create an instance', () => {
    const directive = new HelloworldDirective();
    expect(directive).toBeTruthy();
  });
});
